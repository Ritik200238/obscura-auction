{
  "name": "@obscura/bot",
  "version": "0.1.0",
  "private": true,
  "description": "Auction monitoring bot for Obscura protocol — tracks Dutch prices, English bids, sealed-bid deadlines",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/index.js",
    "dev": "tsc && node dist/index.js"
  },
  "dependencies": {
    "dotenv": "^16.5.0"
  },
  "devDependencies": {
    "typescript": "~5.9.3",
    "@types/node": "^24.12.0"
  }
}
